<h2>Profile Page</h2>

<h3>Welcome to your PROFILE</h3>

<h4>Here are your saved recipies</h4>

<% if(user.bins) { %>
    <% user.bins.forEach(folder => { %>
        <a href="#"><%= folder.userTitle %> </a>
    <% }) %> 
<% } %> 
